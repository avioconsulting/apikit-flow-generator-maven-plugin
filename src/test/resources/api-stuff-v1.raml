#%RAML 1.0
title: Test API
mediaType: application/json
version: v1
baseUri: http://localhost/api/{version}
securitySchemes:

uses:
  rt: ref_type.raml

traits:
  logging: !include somedir/sometrait.raml

/endpoint:
  description: Submits stuff
  post:
    body:
      application/json:
        type: rt.foo
    responses:
      202:
        description: The request has been submitted in the right format and is queued for processing.
        body:
          application/json:
            type: rt.foo
